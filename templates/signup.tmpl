<div class="uk-vertical-align uk-text-center uk-height-1-1">
<div class="uk-vertical-align-middle" style="width: 250px;">

	<form id="form" class="uk-panel uk-panel-box uk-form" method="POST">
		<div class="uk-form-row">
			<input tabindex="1" id="login" name="login" class="uk-width-1-1 uk-form-large" type="text" placeholder="Username">
		</div>
		<div class="uk-form-row">
			<input tabindex="2" id="password" name="password" class="uk-width-1-1 uk-form-large" type="password" placeholder="Password">
		</div>
		<div class="uk-form-row">
			<input tabindex="2" id="password2" name="password2" class="uk-width-1-1 uk-form-large" type="password" placeholder="Password again">
		</div>
		<div class="uk-form-row">
			<input class="uk-width-1-1 uk-button uk-button-primary uk-button-large" type="submit" value="Sign-up">
		</div>
	</form>

</div>
</div>

<script>
document.querySelector('html').style.height = '100%';
document.querySelector('body').style.height = '100%';

/*
var form = document.getElementById('form');
var login = document.getElementById('login');
var password = document.getElementById('password');

form.onsubmit = function(event) {
	event.preventDefault();
	var r = new XMLHttpRequest();
	r.open("POST", "/login", true);
	r.onreadystatechange = function () {
		if (r.readyState != 4 || r.status != 200) return;
		var json = JSON.parse(r.responseText);
		form.parentNode.removeChild(form);
		console.log("Success:", json);
		//run(json.Id, json.Host);
	};
	var params = "login="+encodeURIComponent(login.value)+"&password="+encodeURIComponent(password.value);
	r.setRequestHeader("Content-type", "application/x-www-form-urlencoded");
	console.log(params);
	r.send(params);
}
*/
</script>
